## .NET
bin/
obj/
*.user

## Rider / VS
.vs/
.idea/
*.suo

## OS
.DS_Store

## Secrets
*.env
appsettings.*.local.json

## Local runtime data (LiteDB file, Data Protection key ring) — never commit real router
## credentials or encryption keys, even encrypted-at-rest ones.
src/NetPilot.Agent/data/
src/NetPilot.Web/data/
## Repo-root data/ — used for local (non-Docker) runs where Agent+Web share one absolute
## NetPilot__DataDirectory path instead of Docker's mounted volume.
/data/
